/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['kagqxukzunpizemvxjro.supabase.co'], // Replace <project-id> with your actual project ID
  },
}

module.exports = nextConfig;